{
  "name": "example-package",
  "version": "1.0.0",
  "description": "An example package that runs a script after yarn install",
  "main": "index.js",
  "scripts": {
    "postinstall": "echo 'oops' && exit 1"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
